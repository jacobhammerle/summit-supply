Test the offline sync flow.

1. Open the app. Tap "Trail logs".
2. Turn the "Simulate offline" toggle ON.
3. Tap "Add trail log" three times.
4. Verify all three logs show "Pending sync".
5. Turn the "Simulate offline" toggle OFF.
6. Wait 3 seconds, then take a new snapshot.
7. Verify the screen shows "3 of 3 logs synced".

Pass condition: the text "3 of 3 logs synced" is visible.
